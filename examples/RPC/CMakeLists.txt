add_executable(RPC RPC.cpp)
target_link_libraries(RPC PRIVATE RPCSim::RPCSim CLI11::CLI11 EcoMug)
install(TARGETS RPC LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}" ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}" RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}")
